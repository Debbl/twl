'use client'
import { clsx as __twl_clsx } from 'twl';


const state = { 'p-2': true }

// The injected `clsx` import has to land after the directive, not before it.
export const result = `flex ${__twl_clsx(state)}`
